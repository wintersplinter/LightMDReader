/* Sample document for the working copy. Exercises the things the POC could not
   render: footnotes, reference-style links, and definitions that live away
   from the text that uses them. */
window.SAMPLE_MARKDOWN = [
  "# Block editing, with definitions",
  "",
  "Only the block under the cursor is shown as Markdown. Everything else stays",
  "rendered. Click a paragraph to see its source, then click somewhere else.",
  "",
  "This document uses a reference-style link to [the parser][mdit] and a",
  "footnote.[^why] Both are defined somewhere else in the file, and both still",
  "resolve, because the whole document is parsed once and each block is rendered",
  "from its own slice of that single token stream. ((::This paragraph carries a",
  "hidden comment. It renders as nothing at all, so the hollow red ring in the left",
  "margin is the only sign that it is here. Click the block to read it.::))",
  "",
  "[mdit]: https://github.com/markdown-it/markdown-it",
  "",
  "## Definition blocks",
  "",
  "The dimmed lines are **definition blocks**. markdown-it swallows them into its",
  "environment during the parse, so they produce no token and no rendered output",
  "at all. They are shown in place, as their own blocks, because they are real",
  "lines in your file: click one to edit it, empty it to delete it, or cut it and",
  "paste it somewhere else. The footnote below moves with it.[^move]",
  "",
  "[^why]: A footnote definition has no line map of its own. Without the",
  "    definition-block rule it would be preserved in the file but unreachable in",
  "    the editor.",
  "",
  "[^move]: Try it. Cut this line, put it after the last paragraph, and watch the",
  "    generated list at the bottom renumber itself.",
  "",
  "## What counts as a block",
  "",
  "A block is one top-level Markdown construct, taken from the parser's own line",
  "map — or one run of lines the parser claimed for its environment instead. A",
  "blank line separates paragraphs, so a *double enter* does create a new block,",
  "but a blank line inside a fenced code block does not.",
  "",
  "- A whole list is one block, including its nested items",
  "    - like this one",
  "    - and this one",
  "- Editing the list gives you its full Markdown source",
  "",
  "```python",
  "def blocks(source):",
  "    \"\"\"This fence contains a blank line.\"\"\"",
  "",
  "    return [b for b in parse(source) if b.top_level]",
  "```",
  "",
  "> A blockquote is one block too.",
  "> Both of its lines belong to it.",
  "",
  "| Construct        | One block? | Rendered output |",
  "| ---------------- | ---------- | --------------- |",
  "| Paragraph        | yes        | yes             |",
  "| List             | yes        | the whole list  |",
  "| Code fence       | yes        | yes             |",
  "| Link reference   | yes        | none — dimmed   |",
  "| Footnote def     | yes        | none — dimmed   |",
  "| Footnote list    | no         | generated       |",
  "",
  "---",
  "",
  "### Things to try",
  "",
  "1. Click through every block without typing. The **Document** stat must stay",
  "   `unchanged` and the commit log must stay empty.",
  "2. Click a footnote in the generated list at the bottom — it jumps to the",
  "   definition block that produced it.",
  "3. Cut a footnote definition and paste it in a new block elsewhere. The",
  "   numbering follows the document order, not the order you wrote them in.",
  "4. Empty a definition block. The footnote disappears from the list, and the",
  "   reference in the text falls back to plain `[^label]` text.",
  "5. Press `Esc` while editing: the change is thrown away.",
  "",
  "The highlight ==syntax== is here to prove the renderer is the real one, and the",
  "orange dot is this app's own `((:comment:))` syntax.((:A visible comment gets a",
  "filled dot where it stands. A hidden one gets a hollow ring in the margin. Both\nin the app's own comment red.:))",
  "",
].join("\n");
