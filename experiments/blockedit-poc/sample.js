/* Sample document for the proof of concept. Kept in a script rather than a
   .md file so the page also works when opened straight off disk. */
window.SAMPLE_MARKDOWN = [
  "# Block editing",
  "",
  "Only the block under the cursor is shown as Markdown. Everything else stays",
  "rendered. Click a paragraph to see its source, then click somewhere else.",
  "",
  "The rule that keeps this safe is simple: leaving a block compares the text in",
  "the editor with the text that went in. ((:If they are identical, nothing is",
  "written at all -- not even a re-parse.:)) Only a real difference is committed.",
  "",
  "## What counts as a block",
  "",
  "A block is one top-level Markdown construct, taken straight from the parser's",
  "own line map. A blank line separates paragraphs, so a *double enter* does",
  "create a new block -- but a blank line inside a fenced code block does not,",
  "and that distinction is why the parser decides instead of a blank-line split.",
  "",
  "- A whole list is one block, including its nested items",
  "    - like this one",
  "    - and this one",
  "- Editing the list gives you its full Markdown source",
  "",
  "```python",
  "def blocks(source):",
  "    \"\"\"This fence contains a blank line.\"\"\"",
  "",
  "    return [b for b in parse(source) if b.top_level]",
  "```",
  "",
  "> A blockquote is one block too.",
  "> Both of its lines belong to it.",
  "",
  "| Construct   | One block? | Notes                        |",
  "| ----------- | ---------- | ---------------------------- |",
  "| Paragraph   | yes        | separated by a blank line    |",
  "| List        | yes        | the whole list, not per item |",
  "| Code fence  | yes        | blank lines stay inside      |",
  "| Table       | yes        | header and body together     |",
  "",
  "---",
  "",
  "### Things to try",
  "",
  "1. Click through every block without typing. The **Document** stat must stay",
  "   `unchanged` and the commit log must stay empty.",
  "2. Put a blank line in the middle of a paragraph: it splits into two blocks.",
  "3. Press `Backspace` at the very start of a block: it merges into the one above.",
  "4. Press `Esc` while editing: the change is thrown away.",
  "",
  "The highlight ==syntax== and `{.attrs}` are here to prove the renderer is the",
  "real one, and the orange dot above is this app's own `((:comment:))` syntax.",
  "",
].join("\n");
